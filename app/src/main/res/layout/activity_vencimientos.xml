<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/contenedorPantallaVencimientos"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/fondo_oscuro"
    android:padding="22dp">

    <Button
        android:id="@+id/btnVolverVencimientos"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:text="← Volver"
        android:textColor="@color/blanco"
        android:textStyle="bold"
        android:backgroundTint="@color/azul_principal" />

    <LinearLayout
        android:id="@+id/headerVencimientos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="18dp">

        <ImageView
            android:id="@+id/imgIconoVencimientos"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@android:drawable/ic_dialog_alert"
            android:contentDescription="Vencimientos" />

        <TextView
            android:id="@+id/txtTituloVencimientos"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Vencimientos del día"
            android:textColor="@color/blanco"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginStart="8dp" />

        <TextView
            android:id="@+id/txtMenuVencimientos"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="☰"
            android:textColor="@color/blanco"
            android:textSize="26sp" />

    </LinearLayout>

    <TextView
        android:id="@+id/txtSubtituloVencimientos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Socios cuya cuota vence en la fecha actual"
        android:textColor="@color/gris_medio"
        android:textSize="14sp"
        android:layout_marginTop="16dp" />

    <TextView
        android:id="@+id/txtEstadoVencimientos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@color/gris_medio"
        android:textSize="14sp"
        android:layout_marginTop="12dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerVencimientos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="14dp" />

</LinearLayout>